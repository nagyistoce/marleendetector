import PageHTML

class ImageListHTML(PageHTML.PageHTML):
    
    def __init__(self):
        PageHTML.PageHTML.__init__(self)
        
        
        def getResponse(self):
            lines = []
            return '\n'.join(lines)